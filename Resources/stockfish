Placeholder file: Stockfish engine binary is not included in this repo.

Obtain the real engine binary:
1. Download from https://stockfishchess.org/download/
2. Choose the macOS or Linux build (whichever matches your target environment)
3. Rename/extract it to `stockfish`

Make it executable (macOS/Linux):
chmod +x stockfish

Placement:
EndgameMaster/Resources/stockfish

When you replace this file with the real binary, the app will load it at runtime via:
Bundle.main.path(forResource: "stockfish", ofType: nil)

